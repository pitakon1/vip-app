# 对标贝壳：能力分层、真实差距与落地路径

> 前提：网站 + App 的定位 = **对标贝壳找房**
> 现有项目：`E:\work\app\vip app`
> 配套文档：《老站功能对比-覆盖缺口清单.md》（老站视角）——本文是**架构视角**，结论优先级更高。
> 结论来源：贝壳公开机制（新华社瞭望/贝壳官方/全国工商联报道）+ 泰国市场实测（DDproperty / Hipflat / FazWaz / PropertyScout 等竞品资料）+ 本项目源码字段级盘点。

---

## 一、先给三句判断

1. **定位换成贝壳之后，上一轮"内容层可以砍"的选项被否决了。** 贝壳的核心资产恰恰是楼盘字典 + 内容层，不是它长得像不像个 App。老站那五套字典（楼盘/小区/学校/开发商/资讯）不是"另一套东西"，它就是贝壳路线上**必须继承的前置资产**。
2. **这个项目已经在贝壳最硬的两个地基上打好了桩**：一房一档（`Property.dedupe_key` 唯一索引 + `Listing` 1:N 上架单）和三层判重 + 人工复核闭环（`dedupe_service.py` + `PropertyDedupeReview`）。**这两样在泰国市场是稀缺品**——泰国主流平台恰恰没有。
3. **但对标贝壳还有两个断层，不补就不成立**：ACN 的角色体系只做到"房源方/客源方"二分（分不了贡献度），真房源缺"验真/实勘记录 + 周期性核验"（房源会烂在库里）。

---

## 二、贝壳到底在卖什么：拆成 7 层

贝壳 = 链家 20 年经验的产品化。按护城河从深到浅：

| 层 | 贝壳的东西 | 关键事实 |
|---|---|---|
| **L1 数据底座** | **楼盘字典** | 7 级门址编码（房屋/楼层/单元/楼幢/楼盘/城区/城市）。截至 2024 年底收录 **2.7 亿套**房屋真实数据，累计投入超 **6 亿元**，覆盖 121 城 24 万小区 |
| **L2 规则层** | **ACN 经纪人合作网络** | 2014 年链家启动。一笔交易拆成 **10 个角色**（房源录入、钥匙维护、实勘、委托备案、带看讲解、成交…），佣金按**贡献度**分配。一单最多 **13 名经纪人**协作。人均收入 +28%。已吸引德佑、21 世纪不动产等 **200+ 品牌**加入 |
| **L3 信任层** | **真房源验真** | 房源全生命周期管理，**7×24 实时比对 42 大类特征**，智能模型打分，用 Link/A+ 系统调度经纪人维护更新。结果：虚假房源投诉率 **-95%**，二手房交易周期 **-40%** |
| **L4 体验层** | VR 看房 4.0、智能估价、交易风险预警（20+ 应用） | 自研三维重建，VR 是行业标杆 |
| **L5 C 端产品层** | 找房矩阵（地图/地铁/学区/小区/条件）、房价行情、小区详情、问答社区、经纪人主页与评价 | 流量入口 |
| **L6 B 端作业层** | Link / A+ 经纪人作业系统 | 带看、维护盘、客户跟进、房态维护 |
| **L7 交易与增值层** | 线上签约、资金存管、贷款、家装（被窝）、保洁 | 变现 |

**关键认知：贝壳最深的护城河是 L1 + L2 + L3 这三层，而这三层都不是"做界面"能追上的。** L2 尤其——它本质是**一套利益分配规则**，不是功能。技术只是执行工具。

---

## 三、现有项目在贝壳坐标系里的位置

| 层 | 贝壳 | 现有项目 | 差距 | 必须补？ |
|---|---|---|---|---|
| L1 楼盘字典 | 7 级门址，2.7 亿套 | **约 4 级**：城市/区 → 楼盘 → 楼栋(字符串) → 房号。无独立 Building/Unit 实体，地址未结构化 | 缺单元级、缺房源级结构化门址 | **要补核心**，不必补到 7 级 |
| L1 内容字典 | 小区库 + 内容生态 | **无** School / Developer / Article 实体；`Project.developer` 是字符串 | 三套字典全缺 | **要补**（见第五节） |
| L2 ACN | 10 角色 + 贡献度分佣 + 200 品牌 | **只有 2 分**：`BrokerPartner.broker_role`（客源分销/房源上架/both）+ `Listing.mandate_type`（独家/非独家）+ `split_option`（65:35/50:50/30:70/20:80）。`SplitDeal.participant_role` 是自由字符串，前端仅 4 值。**`Property` 上没有任何"录入人/维护人"外键** | 缺可结算的多角色模型 | **要补，这是最大断层** |
| L3 真房源 | 42 类特征实时比对 + 智能打分 + 实勘 | 判重**有且做得不错**：`dedupe_service.py` 三档（结构化唯一键 / 地址哈希 / 相似度 0.90 阈值 + 房号强一致），配套 `PropertyDedupeReview` 人工队列（pending→merged/dismissed/blocked）。**验真/实勘无表，房源无周期性核验，`ListingStatus.expired` 无自动流转代码** | 缺"验真记录 + 保鲜机制" | **要补，且成本低**（判重已跑通） |
| L4 VR | VR 4.0 + 智能估价 | 有 `video_url` 字段 + `Valuation`(AVM) 模型；无 3D/全景 | 缺 3D/全景 | 可后置 |
| L5 C 端找房 | 地图/地铁/学区/小区/条件 全矩阵 | 条件筛选 ✔、地图 ✔（`/properties/map-points`）、地铁站 ✔（但**仅字符串关键词匹配**，非空间计算）、**学区 ✘**、**小区独立入口 ✘**、商圈 ✘ | 缺学区、小区入口 | **要补**（学区是泰国第一决策因子） |
| L5 房价行情 | 小区均价/成交价/走势/历史成交 | 小区均价 ✔（局部：同项目租金均值对比）、房贷计算器 ✔；**价格走势图 ✘**、**历史成交 ✘**；`MarketIndex/MarketReport` 只回 `published=True` 且**仅内部页使用** | 缺 C 端房价频道 | **要补**（泰国竞品 Hipflat 就靠"楼栋级价格走势"立足） |
| L5 经纪人 C 端 | 主页 + 评分 + 评价 + 带看量 + 成交记录 | **几乎为 0**。`brokers.py` 列表接口限 `STAFF_ROLES`；`Listing` 有 `broker_company/real_name/phone/wechat/line/whatsapp` 字段但 **C 端页面全都不渲染** | 缺公开经纪人身份 | **要补**（ACN 的信任前提） |
| L6 B 端作业 | 带看/维护盘/客户跟进/房态维护 | 带看 ✔（`viewing_appointment.py`，5 状态）、客户跟进 ✔（`Lead.stage` 5 阶段 + CRM 看板）；**维护盘 ✘**（无 maintainer 字段）、无看房反馈字段 | 缺"维护盘" | **要补**（ACN 的落地载体） |
| L7 交易 | 签约/存管/贷款/家装 | 签约 ✔、**Escrow 资金托管 ✔**、按揭 ✔、合同 ✔ | 家装无 | **不补**（泰国不需要） |
| — 本地化 | 贝壳没有 | **多市场配置（`MarketConfig`）、本地支付渠道、PDPA 合规三件套、中英泰三语** | — | **这是差异化，守住** |

---

## 四、泰国市场：哪些能搬，哪些搬不动

### 一个反直觉但很重要的结论：ACN 在泰国比在中国更好落地

实测泰国市场现状：

- 主流平台全是**聚合型房源站**：DDproperty（库最大）、Thailand-Property/Dot Property、Hipflat（楼盘级价格走势 + 地图）、FazWaz（混合型，自营经纪团队 + 验证库存 + 3D 户型）、PropertyScout（20 万+ 房源、3D 虚拟看房、150+ 经纪人）。
- **通病被反复点名**：重复房源、过期房源、钓鱼房源泛滥；**同一套房被不同代理挂在多个平台、价格还不一样**；平台只负责"把门指给你"，不帮你比价、不帮你谈判；LINE 上私人经纪人网络无价格透明度。
- 佣金结构：**房东付佣**，12 个月租约付 1 个月租金、6 个月付半个月。经纪人以独立/精品代理为主。

这意味着两件事：

1. **泰国天然就是"跨店联卖"场**。中国做 ACN 要先把经纪人从"藏盘"教育成"共享盘"；泰国因为**没有独家盘概念**，同一套房本来就由多个代理同时卖——ACN 式的分佣协作对泰国是**刚需解决方案，不是需要教育的新概念**。这是可搬运性最高的部分。
2. **泰国没有"贝壳"**。没有任何一家把「楼盘字典 + ACN 式分佣协作 + 真房源验真」三件套合起来。FazWaz 和 PropertyScout 在"验真"上有动作，但是**自营式**（自己雇 150 个经纪人），不是**网络式**（200 个品牌共享房源）。这个空位是真实的。

### 搬不动的部分

| 贝壳的东西 | 泰国适配性 | 说明 |
|---|---|---|
| 家装/家居/保洁（被窝） | ✘ 不搬 | 阶段太早，且本地供应链不同 |
| 全国 2.7 亿套楼盘字典 | ✘ 不搬 | 泰国只需覆盖 曼谷/芭提雅/普吉/清迈/华欣 等外国人集中市场 |
| 独占式房源规则 | ✘ 要改 | 泰国是房东付佣 + 多家联卖，独家委托要谨慎设计 |
| 外国人土地产权限制 | 贝壳没有，**本地必需** | 公寓永久产权 vs 土地/别墅 30 年租赁权、"外国人公寓配额 49%"等——这是必须进字段的合规项 |

---

## 五、两个已经打好的地基（守住，这是稀缺资产）

### 地基一：一房一档

`Property.dedupe_key` 是 `unique=True` 的唯一索引，配套 `dedupe_type`（project / free_address / none）与 `address_norm`；`Listing` 是 1:N 上架单，带 `dedupe_state` 与 `merged_into_listing_id`。

**"同一套房平台上一份档案、多渠道多份上架单"——这个数据模型正是贝壳"一房一档"的做法。** 泰国现有平台全部是"一房多帖"，这是本项目的结构性优势。

### 地基二：三层判重 + 人工复核闭环

`backend/app/services/dedupe_service.py`：
- 结构化键：`project_id|norm_building|norm_room`
- 自由地址键：`sha256(normalize_address|norm_room)[:32]`
- 相似度：`0.6×归一化编辑距离 + 0.4×bigram Jaccard`，阈值 0.90，且**房号必须一致才进候选**

发布时强命中 → `blocked`；仅相似 → `suspect` + 写 `PropertyDedupeReview(pending)`；人工 `/merge` → 候选单 `merged`、主档保留，`/dismiss` → 回 `new`。前端有 `DedupeReview` 页面。

**这套东西泰国没人做。** 把它往前推一步就是贝壳的 L3。

---

## 六、两个必须补的断层

### 断层一：ACN 角色模型（最大断层）

现状：只有"房源方 / 客源方"二分。`Property` 上**没有任何"录入人 / 维护人"外键**，因此一单里多个经纪人的贡献无法落账，`SplitDeal` 只能在成交后按自由 role 拆一次。

贝壳要做到的：一笔交易拆成 10 个可结算角色，每个角色有人、有权重、有分成。

**建议的最小可行版本（不要一上来做 10 角色）：**

在 `Property` 上加角色外键 + 建一张协作表：

```
Property 增加：
  created_by_partner_id      # 房源录入人
  maintainer_partner_id      # 房源维护人（对应"维护盘"）
  surveyed_by_partner_id     # 实勘人
  verified_at / verified_by  # 核验人与时间

新建 PropertyContribution（房源贡献记录）：
  property_id / partner_id / role(enum) / weight / created_at / evidence
  role ∈ {lister, maintainer, surveyor, key_holder, viewer, closer, client_side}
```

配套改 `SplitDeal`：从"自由字符串 role + 一次拆分"改成**按角色权重规则计算**，并把 `commission_rates.resolve_commission_rate()` 的优先级链（员工 > 部门 > 分销商 > 全局 > `broker.base_rate`）扩展成**"角色权重 × 主体费率"**。

为什么先做 7 个角色而不是 10 个：泰国实际交易链比中国短（多为租赁、少见复杂产权过户），7 个够覆盖 90% 场景。

### 断层二：真房源的"保鲜机制"

现状：判重做得好，但**没有验真记录、没有周期性核验**。`ListingStatus.expired` / `SaleListing.status=expired` 枚举存在却**没有任何自动流转代码**，`backend/app/tasks/` 只有租约到期、租金提醒、备份、对账。

**最小可行版本：**

1. 新建 `PropertyVerification`（房源核验记录）：`property_id / listing_id / method(agent_onsite|owner_confirm|photo_geotag|system) / result / photos / geo / verified_by / verified_at / next_due_at`
2. 加定时任务 `revalidate_listings`：超过 `next_due_at` 未核验 → 标记待核验 → 再次超期 → 自动 `expired`。**这一步直接把"过期房源"这个泰国行业公害变成你的卖点。**
3. 核验结果做**C 端标识**："已实勘 / 已核验 / N 天前核验"——贝壳的信任感就是这么长出来的。

---

## 七、落地顺序

原则：**先做"能形成信任标识"和"能落账分佣"的，后做"好看"的。**

### P0（对标成立的前提，不做就不算贝壳路线）

1. **ACN 角色模型**（`Property` 角色外键 + `PropertyContribution` + `SplitDeal` 按权重计算）
2. **真房源保鲜机制**（`PropertyVerification` + `revalidate_listings` 定时任务 + C 端核验标识）
3. **楼盘字典补级 + 三套内容字典**：`Building` 实体（楼栋结构化）、`School`（含经纬度，接 `geo.py` 算学区距离）、`Developer`（独立实体，替代字符串）、`Article`（6 栏目 CMS）
4. **小区独立入口页**：`Project` 已是主数据，补 8 个字段（占地/户数/车位/栋数/楼层/管理费/基金费/竣工）+ 开 C 端列表与详情页（带"售N间/租N间/最低最高价"聚合）

### P1（把已有能力接到前台）

5. 经纪人 C 端主页 + 公开名录 + 评分/评价/带看量/成交记录（`Listing` 里的 broker 字段已经在库里躺着，只是没渲染）
6. C 端房价频道：小区均价、**价格走势图**（Hipflat 靠这个立足）、**历史成交记录**
7. 学区找房入口 + 地铁站找房从"字符串匹配"升级为**空间计算**（`geo.py` 已有距离能力）
8. 房源分级与"必看好房"（现在 `Home` 的"精选房源"其实只是 `page_size=8` 取前 8 条）
9. 房源详情页转化件：对外编号、浏览量、留资弹窗、双币并排

### P2（长期）

10. 维护盘（B 端作业）、看房反馈字段、VR/3D
11. SEO：SSR / 预渲染 + 结构化 URL。**这条对"对标贝壳"是被低估的**——贝壳的流量一半来自搜索，React SPA 裸跑等于放弃自然流量。

### 明确不做

家装/家居/保洁；7 级门址全覆盖；全国性楼盘字典铺量。**这三件要么不适用，要么投入产出比不成立。**

---

## 八、必须当面说的三条风险

1. **贝壳的护城河是线下经纪人规模，不是软件。** 贝壳背后是链家 20 年、200+ 品牌、数十万经纪人、GTV 3.5 万亿。软件做得再像，**没有 300–500 个真实签约经纪人和 3–5 万条真房源，平台双边网络效应起不来**。对标贝壳的第一投入永远在 BD 和地推，不在产研。这一点如果把优先级排错，软件会被做得很漂亮然后没人用。

2. **ACN 是政治问题，不是技术问题。** 共享房源的前提是经纪人放弃"藏盘"。泰国中小代理愿不愿意把主力房源录进平台、接受平台分走一层？这决定 ACN 能不能立起来。**建议先找 3–5 家愿意做"联卖"的代理做小闭环验证，而不是先开发全套分佣规则。**

3. **数据合规要提前。** 项目已有 PDPA 三件套（Consent / DataSubjectRequest / PrivacyPolicyVersion），但房源实勘照片涉及业主隐私、经纪人手机号属于个人信息。ACN 上线后**跨品牌共享房源数据**，合规定义必须同步做，否则是给未来埋雷。

---

## 九、需要你拍板的三件事

1. **第一批做哪三个？** 我的建议是 **ACN 角色模型 + 真房源核验 + 学校/学区**。理由是：前两个把"贝壳路线"立住且成本低（判重已跑通），第三个换自然流量且泰国无竞品。你要是不同意，告诉我你的排序，我按你的改。
2. **ACN 走不做独家？** 泰国是房东付佣 + 多家联卖。独家委托能提高平台控制力，但会劝退中小代理。**建议：非独家为主（保留现有 `NonExclusiveSplit` 档位），独家只作为"必看好房"的进场条件**——用流量激励换独家，而不是用规则强制。
3. **老站数据迁不迁？** 对标贝壳路线下，这是**必须继承的资产**：老站 6000+ 小区、107+ 学校、上万条房源，正是贝壳 L1 楼盘字典的起点。不迁 = 从零攒字典，成本高一个数量级。迁移方式和范围我可以出方案。
