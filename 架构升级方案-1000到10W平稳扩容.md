# 架构方案 —— 1000 → 1W → 10W DAU 平稳扩容（少改架构与代码）

> 文档版本：v1.0 ｜ 日期：2026-08-24
> 核心诉求：**全程以"复制实例 + 云托管服务"为主，避免架构重写与大量代码改造**
> 配套：`架构升级方案-1000DAU.md`、`架构升级方案-1WDAU.md`、`DEPLOYMENT.md`

---

## 结论摘要（TL;DR）

- **扩容哲学**：应用侧几乎无状态（JWT 无状态 + 连接串已参数化 + Celery 独立），因此 **10W 之前全程靠"加实例 + 换托管服务"，不重写架构、不改业务代码**。
- **全程唯一必须的代码改动**：照片从"本地文件系统"改为"对象存储 OSS/MinIO"（否则多实例文件不一致）。改这一处，其余全是配置与部署层面的操作。
- **三个阶段**：单机(1000) → 多实例+读写分离(1W) → 集群+CDN+队列削峰(10W)。
- **月成本**：1000 ≈ ¥300~600 ｜ 1W ≈ ¥1,400~2,600 ｜ 10W ≈ ¥8,000~15,000。

---

## 一、为什么可以"少改架构和代码"（现状已具备的扩容基础）

| 现状 | 对扩容的意义 |
|---|---|
| JWT 无状态认证，无内存 Session | 任意复制应用实例，无需共享会话 |
| `DATABASE_URL` / `REDIS_URL` 走环境变量（[config.py](file:///e:/app/vip-app-main/backend/app/config.py)） | 换云数据库/云 Redis 只改连接串，零代码 |
| DB 连接池、workers 已参数化（[db.py](file:///e:/app/vip-app-main/backend/app/db.py)、[Dockerfile](file:///e:/app/vip-app-main/backend/Dockerfile)） | 扩容时按需调整，不改代码 |
| 热点接口已有 Redis 缓存（[cache.py](file:///e:/app/vip-app-main/backend/app/core/cache.py)） | 读压力天然被缓存吸收 |
| Celery 独立进程 + 队列路由 | 异步任务可单独扩，不占用 API 资源 |
| **唯一例外**：照片存本地 `uploads/`（[properties.py](file:///e:/app/vip-app-main/backend/app/api/v1/properties.py#L241)） | **多实例前必须先迁对象存储** |

---

## 二、三个阶段（触发条件 → 动作 → 成本）

### 阶段 1：1000 DAU（当前）—— 单机单体
**形态**：1 台 ECS（Nginx + FastAPI + PG + Redis + Celery 同机），前端已生产化。
**成本**：¥300~600/月。

### 阶段 2：1W DAU —— 复制实例 + 读写分离
**触发点**：单机 CPU >70% / 并发 >300 / 读请求变慢。
**动作**（全部为部署/配置操作，无业务代码改动）：
1. 先做**唯一代码改动**：照片迁 OSS/MinIO（见第四节）
2. 应用复制为 **2 台**（同一镜像，只改连接串环境变量），前面加 **Nginx/SLB 负载均衡**
3. PG 迁**云数据库高可用 + 1 只读副本**，浏览读走副本（配置级，代码不变）
4. Redis 升级 **4G 主从**，与 Celery 解耦（独立小实例）
5. 静态/图片挂 **CDN**
**成本**：¥1,400~2,600/月。

### 阶段 3：10W DAU —— 集群 + 全量 CDN + 队列削峰
**触发点**：1W 后继续增长 / 峰值 QPS >1,000 / 单库写压力显现。
**动作**（以托管服务横向扩展为主，代码改动仍然极少）：
1. 应用实例扩到 **4~6 台**（无状态，纯复制，SLB 分发；如需弹性可上 K8s 托管版，代码不变）
2. 只读副本扩到 **2~3 个**，必要时主库升配或按租户/区域拆库
3. Redis 升 **集群 3主3从**；Celery broker 独立
4. **热点搜索/筛选**（房源列表）如 QPS 过高，加 **Elasticsearch**（仅改查询层，业务代码不动）
5. 写入削峰：收租/签约走 **MQ 异步落库**（新增异步路径，不动现有同步逻辑）
6. 多可用区 + 备份 + Prometheus 监控
**成本**：¥8,000~15,000/月。

---

## 三、扩容路径总览图

```
1000 DAU                        1W DAU                       10W DAU
──────────                     ──────                        ──────
单机 ECS                         多实例 + SLB                   4~6 实例 / K8s
├ Nginx+静态                      ├ FastAPI ×2                  ├ FastAPI ×4~6（纯复制）
├ FastAPI×4w                     ├ 只读副本×1                  ├ 只读副本×2~3
├ PostgreSQL                    ├ PG 高可用                   ├ 主库升配/拆库
├ Redis(缓存+broker)            ├ Redis 4G 主从               ├ Redis 集群 3主3从
└ Celery                        ├ Celery 独立 broker          ├ MQ 削峰 + ES 搜索
                                └ CDN                         └ 多可用区 + 监控
  ¥300~600/月                     ¥1,400~2,600/月               ¥8,000~15,000/月

每一阶段到下一阶段：只加实例/换托管服务/改连接串，业务代码保持不动
```

---

## 四、唯一必须的代码改动：照片本地存储 → 对象存储

**为什么必须改**：当前照片保存到 `backend/uploads` 本地目录，且 `main.py` 直接静态挂载该目录。多实例后，上传落在不同实例上，互相看不到文件。

**改动内容（小而集中，仅涉及文件存储层）**：
- 上传改为写入 **OSS / MinIO**（[properties.py](file:///e:/app/vip-app-main/backend/app/api/v1/properties.py#L241-L280) 的 `upload_property_photos`）
- URL 存对象存储路径，删除逻辑同步改
- Nginx `/uploads/` 反代改为指向 OSS 或 CDN（[nginx.conf](file:///e:/app/vip-app-main/frontend-web/nginx.conf) 保留，图片改走外部 URL）
- 涉及代码量：**约 1 个路由 + 1 个存储工具类**，不动业务逻辑

> 若只想极简改造：也可用"**共享 NAS 卷挂载**到所有实例同一路径"替代，代码完全不动，但 CDN 接入不如 OSS 顺滑。**推荐 OSS**，成本低且为 10W 打基础。

---

## 五、各阶段触发点与验证（避免过度提前扩容）

| 阶段 | 触发阈值 | 验证标准 |
|---|---|---|
| 1000→1W | 单机 CPU 持续 >70% / 并发 >300 | 压测 300 并发 P95 < 500ms |
| 1W→10W | 峰值 QPS >1,000 / DB 连接吃紧 / 写入变慢 | 压测 2,000 并发 P95 < 500ms，DB 连接 < 限值 |
| 10W 后 | 单库写瓶颈 / 搜索慢 | 按需拆库 / ES |

> **原则**：不到阈值不加资源，控制成本；到阈值时本方案的每个动作都是"加实例/换服务"，当天即可完成，无需发版改代码（照片迁移除外，提前做好）。

---

## 六、成本与风险提示

- **成本递增是平滑的**：三个阶段分别为 ~¥500 / ~¥2,000 / ~¥10,000 量级，随业务增长逐步投入。
- **主要风险**：
  1. **照片迁移要提前**（阶段 2 前必须完成），否则多实例会文件不一致
  2. 10W 时"少改代码"的天花板：若房源搜索/报表复杂度极高，仍需引入 ES/数仓，这是业务复杂度决定的，非架构能力不足
  3. 分布式运维复杂度：10W 阶段建议至少 1 名专职运维/后端，纯靠"无代码改动"撑住大规模需要可靠的监控
- **明确的底线**：**10W 以内无需上微服务、无需分库分表**（除非单表数据量过大），单体复制 + 托管服务足够，这是"少改架构"能成立的关键。 