# 架构升级方案 —— 支撑 1W DAU（10,000 日活）

> 文档版本：v1.0 ｜ 日期：2026-08-24
> 适用：VIP 租赁管理系统，目标 **10,000 DAU / 峰值约 800~1,000 同时在线**
> 配套：`DEPLOYMENT.md`（含三阶段部署与成本总览），本文聚焦「1W DAU 里程碑」改造动作与服务器选型

---

## 结论摘要（TL;DR）

- **云需要升级扩容：是。** 当前 `docker-compose` 单机单体形态无法承载 1W DAU，且前端跑 vite dev server 不能直接对外。
- **推荐架构档位：介于 `DEPLOYMENT.md` 阶段 2 与阶段 3 之间（“阶段 2.5”）。**
  采用「应用与数据库分离 + 前端 Nginx 静态化 + 连接池收敛 + Redis 缓存热点 + 只读副本」这一档，**暂不引入 K8s**（1W 还用不上，成本翻倍不值）。
- **每月成本预估：¥1,400~2,600**（切准 1W，不含支付通道手续费）。

---

## 一、为什么必须升级（现状诊断）

| # | 现状硬伤 | 危害 | 位置 |
|---|---|---|---|
| 1 | 前端容器跑 **vite dev server** | 单容器、无静态缓存/压缩，任何公网流量直接打爆源服务；开发专用形态 | [frontend-web/Dockerfile](file:///e:/app/vip-app-main/frontend-web/Dockerfile) |
| 2 | DB 连接池 **120 条 > PG 默认 100** | 10k 并发未到，连接就已超限报错 | [db.py](file:///e:/app/vip-app-main/backend/app/db.py#L28-L35) |
| 3 | 单机单体，无负载均衡 | 无冗余、无水平扩展能力 | [docker-compose.yml](file:///e:/app/vip-app-main/docker-compose.yml) |
| 4 | Redis 缓存 / Celery broker 混用 | 热点缓存打满会拖垮异步任务 | [redis_client.py](file:///e:/app/vip-app-main/backend/app/redis_client.py) |
| 5 | 无 CDN | 房源大图直接回源，带宽与延迟双高 | — |
| 6 | 无只读副本 | 浏览读与写共用主库 | — |
| 7 | 有 `/metrics` 但未接监控告警 | 无法看到容量水位 | [main.py](file:///e:/app/vip-app-main/backend/app/main.py#L92-L96) |

---

## 二、1W DAU 的改造动作（分优先级落地）

### P0 —— 不改就上不了线（必做）

1. **前端生产化**
   - 把 `npm run dev` 改为 `tsc -b && vite build`，产物交给 **Nginx** 托管。
   - Nginx 开 `gzip/brotli` 压缩、静态资源 `Cache-Control: public, max-age` 强缓存。
   - 作用：首页/列表/图片静态命中率 >90%，源服务请求量砍掉大半。

2. **收敛 DB 连接池**
   - 每个 worker 的 `pool_size` 从 10 下调（如 5），或统一走 **PgBouncer** 连接池。
   - 保证「workers × 连接池 ≤ PG max_connections 的 70%」。

3. **Redis 缓存热点读接口**
   - 房源列表/详情、看板统计、公开页面等读多写少的接口，加 Redis 缓存（TTL 分钟级），DB 读压力大幅下降。

### P1 —— 支撑 1W 稳定（推荐做）

4. **Nginx 统一入口 + 反向代理**
   - 托管静态资源 + 反代到 FastAPI + 做 TLS 终止、基础限流。

5. **只读副本（读写分离）**
   - 浏览类读请求走只读副本，主库只承受写 + 一致性读取。

6. **Redis 与 Celery 解耦**
   - 缓存专用一个实例；broker/results 独立出去（或换 RabbitMQ）。

7. **对象存储前置 CDN**
   - 房源大图/视频走 CDN，SSD/MinIO 只做源站。

### P2 —— 可观测与弹性（1W 建议并行）

8. **接 Prometheus + 告警**：把已有 `/metrics` 接到云监控/Grafana，设容量告警。
9. **SLB 负载均衡到 2 个 API 副本**：预留水平扩展入口。

> **明确不做（1W 还不值得）**：K8s 集群、分库分表、服务网格、多可用区容灾。这些到 **10W DAU** 再上。

---

## 三、目标架构（1W DAU）

```
                    用户 / App / 微信小程序
                            │
                     ┌──────┴───────┐
                     │  CDN（静态+图片）│ ← Cloudflare / 阿里云 CDN
                     └──────┬───────┘
                            │
                     ┌──────┴───────┐
                     │  Nginx 统一入口 │ TLS / 压缩 / 限流 / 反向代理
                     └──────┬───────┘
                ┌───────────┴───────────┐
                │ SLB 负载均衡           │
                └───────────┬───────────┘
                 ┌──────────┴──────────┐
                 │  FastAPI App ×2     │ 4核8G（Nginx 托管前端静态）
                 └──────────┬──────────┘
                 ┌──────────┴──────────┐
                 │ ① 主 PostgreSQL     │ 写 + 一致性读
                 │ ② 只读副本          │ 浏览读
                 └─────────────────────┘
     Redis（缓存）        PgBouncer（连接池）      OSS（源站）
```

---

## 四、云是否需要升级 + 服务器选型

**结论：需要升级。** 从「单机 docker-compose」→「应用/数据库分离 + 只读副本」档位。
以**阿里云新加坡主节点**为例（理由见 `DEPLOYMENT.md` §2.2），1W DAU 推荐配置：

| 组件 | 规格 | 用途 | 参考月费 |
|---|---|---|---|
| ECS 云服务器 ×2 | 4核8G / 100G SSD / 5Mbps | FastAPI ×2 + Nginx 前端静态 + SLB 后端 | ~¥400–700 |
| 云数据库 PostgreSQL | 4核8G 高可用 + **1 只读副本** | 主数据存储 + 读写分离 | ~¥600–900 |
| 云数据库 Redis | **4G 主从** | 热点缓存（Celery broker 独立或另购小实例） | ~¥150–300 |
| 对象存储 OSS | 200G | 房源图片/合同源站 | ~¥30–60 |
| CDN | 按量（海外 Cloudflare 可免费起步） | 静态资源/大图加速 | ¥20–100 |
| 负载均衡 SLB | L4/L7 基础版 | API 流量分发 | ~¥100–200 |
| 监控（云监控/Prometheus） | 基础版 | 容量水位 + 告警 | ¥0–100 |
| **合计** | | | **¥1,400–2,600/月** |

### 服务器规模换算逻辑（1W DAU）
- 1W DAU / 峰值 20% 同时在线 ≈ **800~1,000 并发在线**，峰值 QPS **约 400–800**。
- 按 `DEPLOYMENT.md` §3.2：4核8G FastAPI 可扛 500–800 并发 / 200–350 QPS → **2 台正好留出冗余**。
- PostgreSQL 4核8G 可支撑 1,500–3,000 读 → 主库 + 1 只读副本读写分离后余量充足。
- **能否更低配**：起步可先 **1 台（App+PG+Redis 合并）8核16G / 100G SSD**，约 ¥1,100/月，稳住后再拆两台 App 滚动升级。

---

## 五、与现状文档的关系与落地顺序

- **DEPLOYMENT.md** 已有三阶段成本/合规框架 → 本文是其「1W 里程碑」的落地细化。
- **建议落地顺序**：P0①前端生产化 → P0②连接池 → P0③Redis 缓存 → P1④Nginx → P1⑤只读副本 → P1⑥Redis/Celery 解耦 → P1⑦CDN → P2⑧监控 → P2⑨SLB 双副本。

---

## 六、验证与里程碑

| 里程碑 | 判定标准 |
|---|---|
| P0 完成 | 前端静态托管可公网访问，连接池不再超限，热点接口走缓存 |
| P1 完成 | 压测 800 并发峰值 P95 延迟 < 500ms，DB 连接保持在限内 |
| P2 完成 | 监控可看到容量水位，双副本滚动发布不停服 |