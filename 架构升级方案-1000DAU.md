# 架构方案 —— 1000 DAU 起步（可平滑扩容到 1W）

> 文档版本：v1.0 ｜ 日期：2026-08-24
> 定位：**从 1000 DAU 起步，架构上预留扩容路径，不做死胡同**，后续可平滑升到 1W DAU
> 配套：`架构升级方案-1WDAU.md`（1W 目标态）、`DEPLOYMENT.md`（三阶段成本/合规框架）

---

## 结论摘要（TL;DR）

- **起步形态**：**单台云服务器**（进程级分离：Nginx + FastAPI + PG + Redis + Celery 跑在一台），前端生产化后用 Nginx 托管。
- **不引入**：K8s、SLB、只读副本、CDN 收费版——1000 DAU 用不上，但**代码与配置层面全部预留**，扩容时零返工。
- **月成本预估：¥300~600**。
- **扩容路径**：先「拆 App 独立一台」→ 再加「只读副本 + PgBouncer」→ 再到「1W 文档」中的阶段 2.5（双 App + 只读副本 + CDN）。

---

## 一、容量估算（1000 DAU）

| 指标 | 值 | 依据 |
|---|---|---|
| 同时在线（峰值 20%） | ≈ **200 并发** | 1000 × 20% |
| 峰值 QPS | **50~100** | 每在线用户约 5~10 个请求/次会话 |
| 带宽 | **5~10 Mbps** | 图片走 CDN 后回源很低 |
| 单机承载 | 2核4G 可扛 200–300 并发 / 80–150 QPS | `DEPLOYMENT.md` §3.2 |

> 结论：**单台 2核4G 起步足够，且有约 1 倍余量**；若图片/房源浏览占比高，建议直接 4核8G。

---

## 二、起步期架构（单机但组件分离）

```
                 用户 / App / 微信小程序
                         │
                 ┌───────┴───────┐
                 │  Cloudflare   │ ← 免费 CDN + DNS（先挂上，图片命中后回源大降）
                 └───────┬───────┘
                         │
              ┌──────────┴──────────┐
              │     单台 ECS          │
              │  ┌────────────────┐  │
              │  │ Nginx          │← 静态前端(生产构建) + TLS + 反代 /api
              │  ├────────────────┤  │
              │  │ FastAPI (uvicorn│← 4 workers（同步 SQLModel 引擎）│  │
              │  ├────────────────┤  │
              │  │ PostgreSQL 15  │   │
              │  ├────────────────┤  │
              │  │ Redis 7        │← 缓存（Celery 暂共用，预留拆分）  │  │
              │  └────────────────┘  │
              └──────────────────────┘
```

**设计要点（为扩容埋好伏笔）：**
1. **进程级分离、目录/端口清晰**：App / PG / Redis 各自独立进程与配置，未来拆机时各自搬到独立 ECS，配置只改连接串。
2. **连接串走环境变量**：`DATABASE_URL`、`REDIS_URL` 已由 [config.py](file:///e:/app/vip-app-main/backend/app/config.py) 支持，拆机零改码。
3. **前端生产化**：vite build 产物交 Nginx，不走 dev server（否则无法公网，也是死胡同）。
4. **域名 + CDN 提前挂好**：Cloudflare 免费版，图片/静态开始缓存，回源带宽从一开始就低。

---

## 三、必须的代码改造（P0，1000 DAU 上线前完成）

> 这些改造**任何规模都需要**，现在做了就是为 1W 铺路，不是浪费。

1. **前端生产构建 + Nginx 托管**
   - 改 [frontend-web/Dockerfile](file:///e:/app/vip-app-main/frontend-web/Dockerfile)：`npm run build` → Nginx 托管 dist，开 gzip + 静态强缓存。
   - 不改则无法公网上线，属于硬伤。

2. **收敛 DB 连接池（防超限）**
   - [db.py](file:///e:/app/vip-app-main/backend/app/db.py#L28-L35) 当前 4 workers × 30 = 120 连接 > PG 默认 100。
   - 起步改为 `pool_size=5, max_overflow=10`（每 worker ≤15，4 workers = 60 < 100），并把 `pool_size` 改成**可由环境变量覆盖**，为将来接 PgBouncer 预留。

3. **Redis 缓存热点读接口**
   - 房源列表/详情、看板等读多写少接口加 Redis 缓存（TTL 分钟级）。
   - [redis_client.py](file:///e:/app/vip-app-main/backend/app/redis_client.py) 已有基础，直接复用。

4. **生产启动命令固定多 worker**
   - 后端 Dockerfile 已用 `--workers 4`（[backend/Dockerfile](file:///e:/app/vip-app-main/backend/Dockerfile)），保留；workers 数改为环境变量可调。

---

## 四、云是否需要升级 + 服务器选型（1000 DAU）

**结论：不需要大升级，单台起步即可。** 以阿里云新加坡轻量应用服务器为例：

| 组件 | 规格 | 用途 | 参考月费 |
|---|---|---|---|
| 轻量应用服务器（新加坡） | **2核4G / 100G SSD / 5Mbps** | Nginx + FastAPI + PG + Redis + Celery 全栈单机 | ¥80–150 |
| 轻量应用服务器（国内） | 2核2G / 50G / 3Mbps | 小程序 API 网关 + ICP 备案 | ¥40–70 |
| 域名 + SSL | 1 个 .com + Let's Encrypt 免费 | 主域名 | ¥5 |
| CDN | Cloudflare 免费版 | 静态/图片加速 | ¥0 |
| 短信/邮件 | 按量 | 验证码/通知 | ¥50–200 |
| **合计** | | | **¥175–425/月** |

> **若图片/房源浏览占比高**，直接上 **4核8G**（约 ¥150–250/月），整体仍在 ¥300–600 区间，余量更大、升 1W 前不用再动 App 这层。

---

## 五、扩容路径（1000 → 1W，分三步，每步零返工）

| 步骤 | 触发点 | 动作 | 成本增量 |
|---|---|---|---|
| **① App 拆独立 ECS** | 单机 CPU >70% 或并发 >300 | App 迁到独立 4核8G，PG/Redis 留原机或迁云数据库 | +¥300–500/月 |
| **② 只读副本 + PgBouncer** | 读请求成为瓶颈 / 连接吃紧 | PG 上 1 只读副本，读写分离；加 PgBouncer 连接池 | +¥400–600/月 |
| **③ 阶段 2.5** | 逼近 1W DAU | 按 `架构升级方案-1WDAU.md` 执行：双 App + SLB + Redis 4G + 收费 CDN | 到 ¥1,400–2,600/月 |

> 关键保障：因为**起步就把连接串参数化、前端生产化、Redis 缓存、组件进程分离**做掉了，上面三步只加机器/服务，**不改代码、不重构**。

---

## 六、验证与里程碑

| 里程碑 | 判定标准 |
|---|---|
| P0 改造完成 | 单机可公网访问，连接池 60 < 100 不再超限，热点接口走缓存 |
| 上线 1000 DAU | 压测 200 并发峰值，P95 延迟 < 500ms，单机 CPU < 60% |
| 触发扩容① | 单机 CPU 持续 >70% / 并发 >300，开始拆 App |
| 平滑到 1W | 按路径执行至阶段 2.5，无需代码重构 |