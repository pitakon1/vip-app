# 老站功能取舍：必补清单（对标贝壳口径下的必要性筛选）

> 输入：《老站功能对比-覆盖缺口清单.md》（老站 20 项功能面）+《对标贝壳-能力分层与落地路径.md》（贝壳七层）
> 口径：**只保留"不做就不成立"的；体验优化类一律不加。**
> 结论时间：2026-09-21
> 结论：**老站 20 项功能面 → 必补 12 项，砍掉 10 项**（含 1 项"工程侧必要但非功能"）

---

## 一、"必要"的判定标准（三条，满足其一才算）

以后争"这个要不要做"，用这三条卡，不靠感觉：

| 代号 | 判据 | 含义 |
|---|---|---|
| **A 数据资产** | 补了会**持续积累**、越用越厚，形成壁垒 | 字典类（学校/开发商/站点/设施）。空库跑一年还是空库，但字典跑一年就是资产 |
| **B 模式前提** | 不补，贝壳路线的某个环节**直接跑不通** | 没有入口 → 供给进不来；没有留资 → leads 是死的；没有经纪人公开页 → ACN 没有信任面 |
| **C 决策必需** | 买方/租方做决策**必须看到**的信息，缺了会误导 | 双币价格、朝向、产权类型、学区距离 |

**不满足 A/B/C 的，一律归为"体验优化"，不做。** 这是本次筛掉 10 项的唯一依据。

---

## 二、必补（12 项）

### A 类 · 数据字典（4 项）——补了会自己长厚

#### 1. 学校库 + 学区距离 ★最高优先级

老站的杀手锏，泰国买房的第一决策因子。老站房源详情页一次列 15 所学校**带实际距离**（0.26~2.08Km），学校页再挂"附近出售/出租"。

**落地**：
- 新建 `School`：`name / name_en / address / district / lat / lng / phone / website / student_count / stage(幼/小/中/大学) / curriculum(IB/美制/英制/泰制) / tuition_range / description / photos`
- `Property` / `Listing` 详情页加"周边学校"区块 —— **复用现有 `backend/app/api/v1/geo.py` 的距离算法**，取最近 5~8 所
- 学校详情页反查"附近在售/在租"
- 回填老站 107 所学校数据

**为什么必要**：贝壳 L5 找房矩阵的标准件 + 泰国市场无竞品 + 现有距离能力可直接复用。这是全清单里投入产出比最高的一条。

#### 2. 开发商库（实体化）

现在 `Project.developer` 是 `Optional[str]` 字符串 —— 无法聚合、无法成页、无法 SEO。

**落地**：
- 新建 `Developer`：`name / name_th / name_en / phone / fax / email / website / address / lat / lng / logo / description_zh / description_en`
- `Project.developer`(str) → `Project.developer_id`(FK)，写数据迁移映射
- `/developers` 列表 + `/developers/:id`（旗下项目列表）

**为什么必要**：楼盘字典的一级实体。字符串→实体的改动越晚做，数据清洗成本越高（老站可回填 26+ 家）。

#### 3. BTS / MRT / ARL 站点字典

现在只有 `Project.nearest_subway` 一个自由文本字段 —— 无法按站点找房。

**落地**：
- 新建 `TransitStation`：`code(E9/E4/S12) / name / name_en / line(BTS/MRT/ARL) / lat / lng / district`
- `Project` 补 `nearest_station_id` + `distance_m`
- 找房从"字符串关键词匹配"升级为**站点半径空间计算**（`geo.py` 已有能力）

**为什么必要**：泰国最硬的地段维度（比区/商圈都细）。现有实现是字符串匹配，等于没做。

#### 4. 物业设施标准字典

现在 `Project.amenities` 是自由 JSON —— 无法筛选。

**落地**：
- `AmenityDict` 字典表（永久产权/泳池/健身房/桑拿房/图书馆/精装/免费停车位/小区花园/会所/共享空间/带家具/BTS或MRT）+ `ProjectAmenity` 关联
- 列表页支持按设施勾选筛选

**为什么必要**：一张字典表的事，但不做，贝壳式"条件筛选"永远是残的。

---

### B 类 · 模式前提（4 项）——不补链路是断的

#### 5. 公开房源详情页（免登录可看）★ 被严重低估

**这条不是"补老站功能"，是补一个现在根本不存在的页面。**

实测证据：
- `frontend-web/src/App.tsx` 公共路由**只有 4 条**：`/`、`/listings`、`/login`、`/register`
- `/properties/detail/:id` 与 `/tenant/properties/:id` **都在 `ProtectedRoute` 内**
- `frontend-web/src/pages/Home/index.tsx:205` 与 `:270`：
  ```tsx
  onClick={() => token ? navigate(detailPath(p.id)) : handleSearch()}
  ```
  —— **未登录点房源卡片，不给看详情，被弹回列表。**

而老站明确"未注册即可看全部房源"，详情页是最重的转化页（房源编号 / 留资弹窗 / 经纪人卡片 / 学区距离 / 房价走势全在这）。

**落地**：新增公共路由 `/listing/:id`，内容 = 相册+视频 / 双币价格 / 面积户型楼层朝向装修 / **小区信息块** / **学区距离** / 地图 / 经纪人卡片 / **留资表单** / 对外编号。同时改掉上面那个三元表达式。

**为什么必要**：老站所有内容资产的价值，都要靠"免登录可浏览"这条管道输出去。**管道现在是关着的**——学校库、开发商库、小区页做得再好，用户点不进去就是零。

#### 6. C 端留资入口

`leads`（`Lead.stage` 5 阶段 + CRM 看板）和 `viewings` 的后端能力都在，但**没有 C 端入口把流量接进去**。

**落地**：详情页"留言咨询（购买意向）"表单 → 写现有 `leads`，加来源归因 `LeadSource.listing_detail`。

**为什么必要**：没有它，CRM 是空转的。这是转化的唯一入口。

#### 7. 经纪人公开名录 + 店铺页

老站：名录页（头像/称呼/电话/公司/售 N 间/租 N 间）+ 店铺页（该经纪人房源列表，带区域/房型/装修/朝向/押金）。

新项目：`brokers.py` 列表接口限 `STAFF_ROLES`，`Listing` 里 `broker_company / real_name / phone / wechat / line / whatsapp` **字段全躺着，C 端一个都不渲染**。

**落地**：
- `/brokers` 名录 + `/brokers/:id` 店铺，数据直接读 `Listing` 已有字段
- 聚合"在售 N 间 / 在租 N 间"
- **V1 不做评分/评价/带看量/成交记录**（新站没有成交数据，评价体系是空的，做了是假数据）

**为什么必要**：ACN 的信任前提 —— 客户得能点进经纪人页面。且成本近零（字段已在库）。

#### 8. C 端发布 / 委托入口

`publish-listing` 现在只挂在 **owner / employee** 菜单下（`roleMenus.ts`），面向公众的免费发布/委托入口**没有**。

**落地**：公开路由 `/publish`（发布房源）+ `/delegate`（委托租售），注册后提交 → 进现有上架审核队列。

**为什么必要**：这是**双边网络的"供"那一侧**。没有它，房源只能靠运营手动录，ACN 的入场券（经纪人愿意录盘）无从谈起。

---

### C 类 · 决策必需（3 项）——不做，信息是错的

#### 9. 双币显示 + 真实汇率（现有汇率是写死的）

`frontend-web/src/lib/money.ts:14`：
```ts
const RATES: Record<string, number> = { THB: 1, CNY: 5.2, USD: 36, EUR: 39, RM: 10 }
```
**硬编码。** 1 CNY = 5.2 THB，而老站首页实时价 1 CNY = 4.9682 THB —— **偏差 4.6%**。给客户报价会报错。

**落地**：
- 后端汇率同步（`/pricing/convert` + `CURRENCY_TO_THB` 已存在，接真实汇率源）
- `money.ts` 的 `RATES` 改为运行时从接口取 + 缓存降级
- 列表卡片 / 详情页价格 **THB + CNY 并排**

**为什么必要**：目标客群是中国客户，只看泰铢没有价格体感。**注意：首页独立的汇率条不做**（属于体验件，并入这里）。

#### 10. 字段口径修正（Property / Project）

缺了会直接误导决策的字段：

**`Property` 补**：
- `orientation` 朝向（enum）—— 泰国西晒是硬伤，租客/买家必看
- `decoration` 装修状况（enum：毛坯/简装/精装/豪装/带家具）—— 替代现在的 `furnished: bool`（一个布尔表达不了老站的"精装修/毛坯/奢华装修"）
- `listing_no` 对外编号（如 FY0027536）—— 客服/经纪人沟通全靠它，成本极低

**`Project` 补**：
- `open_date` 开盘日期、`total_buildings` 栋数、`total_floors` 总楼层、`parking_spaces` 车位
- `management_fee_per_sqm` 管理费、`avg_price` 均价
- `tenure`（freehold/leasehold）+ `foreign_quota_pct`（外国人公寓配额 49%）—— **泰国特有合规项，贝壳没有但这必须有**
- `payment_plan`(JSON) 期房付款方案（定金 / 首付% / 尾款%）—— 期房是分期付款，中国客户最关心付款节奏

**为什么必要**：这些是买方**直接用来决策**的信息。缺了不是"少个标签"，是"信息不成立"。

#### 11. 小区 C 端入口页

`Project` 表已存在，但**没有任何 C 端路由**——它是后台主数据，不是前端产品。

**落地**：`/communities` + `/communities/:id`，卡片带"**售 N 间 / 租 N 间 / 最低最高价**"聚合（老站做法），详情页挂该小区下的在售在租列表。

**必要字段**：户数（有）、竣工年（有）、管理费（→ 第 10 项）、开发商（→ Developer）、楼层、栋数。

**为什么必要**：楼盘字典的**展示面**。字典存了不给用户看，等于没存。

---

### 工程侧（1 项，非功能但必要）

#### 12. SEO 最小实现

不做 SEO，上面 11 项内容资产建了也没流量 —— 贝壳流量一半来自搜索，老站就是靠 SEO 吃自然流量。

**落地（最小）**：结构化 URL（`/listing/:id`、`/communities/:id` 不用 hash）+ 关键路径预渲染（prerender），**不做全站 SSR**。

**顺序**：必须排在公开详情页之后。

---

## 三、砍掉（10 项 + 理由）

| # | 老站有、新项目没有的东西 | 为什么砍 |
|---|---|---|
| 1 | **浏览量 `view_count`** | 体验件。且是**最容易被刷坏的指标**，贝壳都不拿它当核心 |
| 2 | **项目级收藏**（"收藏楼盘"） | 体验件。`favorites.py` 已有房源收藏，够用 |
| 3 | **客厅数 `living_rooms`** | 泰国公寓是 1LDK/2LDK 一体结构，客厅数无决策价值 |
| 4 | **转租 / 合租独立入口** | 这是**筛选维度**不是能力。降级为 `Listing` 上一个枚举字段即可，不做一级入口。泰国合租是低价值市场 |
| 5 | **周边配套 POI**（商场/医院/超市） | 数据成本高（要买 POI 或调地图 API），且**学区距离已覆盖最核心的 POI 需求**。等有预算再说 |
| 6 | **单房源 / 单小区价格走势图** | **新站没有历史数据，画不出来**。要做也得等积累 6 个月真实数据，现在做就是造假图 |
| 7 | **首页独立汇率条** | 已并入第 9 项双币显示。单独一条是体验件 |
| 8 | **EIA 状态 / 维修基金 / 阶梯物业费** | 专业信息，超出中国客户的决策需要。有客户真问了手动补 |
| 9 | **资讯 CMS** | **当前阶段砍。** 贝壳的护城河是字典不是资讯；且没有房源供给规模时，资讯带来的流量**无处转化**（看完文章没有房源可看）。等三套字典上线、有真实房源量后再评估，届时只做 `Article` + 分类 + 列表/详情，**不做评论/推荐位/多级栏目运营后台** |
| 10 | **商业地产专属字段口径 + 独立模板**（莱/平方哇、客房数、厂房面积） | 先只把 `property_type` 枚举扩到含 商铺/写字楼/仓库/工厂/土地/酒店，让房源**能录进来**；字段口径等确认业务真有成交量再做。**盲目定字段口径的返工成本最高** |

**另外砍掉一项工作**：老站后台菜单逐项对比 —— 不做了。老站后台是低配 CMS（栏目/文章/房源/用户），新项目后台（权限矩阵/审核/审计/去重）已经反超，逐项比对是负收益。

---

## 四、比"老站缺什么"更要命的一件事

**必须先解决，否则上面 12 项有一半白做：注册墙。**

现状是"**未登录 = 看不到详情**"（`Home/index.tsx` 那个三元表达式 + 详情页全在 `ProtectedRoute` 内）。而贝壳是**浏览全开放，只在联系/收藏时要求登录**；老站也是"未注册即可看全部房源"。

**这个墙不拆，内容字典建得再厚，也只是给已登录用户看。** 自然流量进不来 = SEO 和内容层全部失效。

**建议**：浏览全开放（列表 + 详情免登录），只在①收藏 ②留资 ③发布 ④联系经纪人 这四类动作上要求登录。这同时也是转化漏斗的正确设计——**让用户先产生兴趣，再要注册**。

**待确认的产品决策**：是否拆墙。这是这次唯一一个"不是补功能、而是改产品形态"的问题。

---

## 五、执行顺序

原则：**先补"能让链路跑通"和"能形成积累"的，最后补"好看"的。**

| 批次 | 内容 | 理由 |
|---|---|---|
| **第 0 批（先决）** | 拆注册墙 + 公开房源详情页 | 不拆，后面全部白做 |
| **第 1 批** | 学校库 + 学区距离 → 留资入口 → 小区 C 端页 | 链路最短闭环：能搜到 → 能点进去 → 能留资 |
| **第 2 批** | 站点字典 → 开发商库 → 经纪人公开页 → C 端发布入口 | 补找房矩阵 + 开供给入口 |
| **第 3 批** | 双币真实汇率 → 字段口径修正 → 设施字典 | 修正错误信息 + 让筛选可用 |
| **第 4 批** | SEO 最小实现 | 前面做完了才有东西可被收录 |

**注**：本清单**不含** ACN 角色模型与真房源核验 —— 那两项来自《对标贝壳-能力分层与落地路径.md》的 P0，是老站**没有**的东西（即：比老站还要往前一步），不在本文"老站→新项目"的补充范围内。

---

## 六、要你拍板的两件事

1. **注册墙拆不拆？** 拆墙是这 12 项的前置条件，也决定 C 端流量能不能起来。建议拆（浏览开放，动作收费/收登录）。
2. **第 1 批是不是就按"学校库 + 留资 + 小区页"走？** 我的排序理由是"最短闭环"，你要改告诉我。

---

## 附：本次新增验证的证据

| 结论 | 证据位置 |
|---|---|
| C 端公共路由只有 4 条 | `frontend-web/src/App.tsx:103-106` |
| 详情页全在鉴权内 | `App.tsx:122`、`App.tsx:183` |
| 未登录不给看详情 | `frontend-web/src/pages/Home/index.tsx:205,270` |
| 汇率硬编码 | `frontend-web/src/lib/money.ts:14` |
| `Property` 有/无哪些字段 | `backend/app/models/property.py:38-73` |
| `Project` 有/无哪些字段 | `backend/app/models/project.py:18-36` |
| 列表页全量拉取前端筛选（`page_size: 999`） | `frontend-web/src/pages/PublicListings/index.tsx:262` |
| C 端列表页调 B 端 `/properties` 接口 | `PublicListings/index.tsx:306`、`Home/index.tsx:64` |
